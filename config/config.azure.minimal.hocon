{
  "input": {
      "topicName": "sp-dev-enriched"
      "bootstrapServers": "localhost:9092"
  }

  "output": {

    "good": {
      ## For local developmemt:
      # "location": "file://"${PWD}"/output"

      "location": "abfs://container1@streeterdev1.dfs.core.windows.net/events"
    }

    "bad": {
      "topicName": "sp-dev-bad"
      "bootstrapServers": "localhost:9092"
    }

  }

  "spark": {
    "conf": {
      "fs.azure.account.key": ${AZURE_STORAGE_ACCOUNT_KEY}
    }
  }
}
